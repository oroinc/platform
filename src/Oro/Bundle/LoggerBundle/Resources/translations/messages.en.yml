oro:
    logger:
        system_configuration:
            groups:
                logger_notification_settings.title: 'Error logs notification'
            fields:
                email_notification_recipients:
                    label: 'Recipients email addresses'
                    tooltip: 'To send notifications to specific people, type their email addresses separated by semicolons (;). Leave this field empty to disable error logs notification. '
