{% extends "OroEmailBundle:Email:activityLink.html.twig" %}


{% block action_controll %}
    {% set options = options|merge({
        'aCss': 'no-hash'
    }) %}
    {% if feature_enabled('email') %}
        {{ UI.clientButton(options) }}
    {% endif %}
{% endblock %}

