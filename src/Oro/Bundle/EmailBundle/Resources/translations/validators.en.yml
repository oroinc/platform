"The template for {{ field }} ({{ locale }}) has syntax error: {{ error }}": "The template for {{ field }} ({{ locale }}) has syntax error: {{ error }}"
"This value contains not valid email address.": "This value contains not valid email address."
"Recipient can not be empty": "Recipient can not be empty"
oro.email:
    validator:
        configuration:
            connection:
                smtp: Could not establish the SMTP connection

        security_policy:
            disallowed_tag: 'The template in {{ field }} field ({{ locale }}) uses a disallowed Twig tag "{{ name }}".'
            disallowed_filter: 'The template in {{ field }} field ({{ locale }}) uses a disallowed Twig filter "{{ name }}".'
            disallowed_function: 'The template in {{ field }} field ({{ locale }}) calls a disallowed Twig function "{{ name }}".'
            disallowed_property: 'The template in {{ field }} field ({{ locale }}) accesses a disallowed property "{{ name }}" on "{{ variable }}" variable.'
            disallowed_method: 'The template in {{ field }} field ({{ locale }}) calls a disallowed method "{{ name }}" on "{{ variable }}" variable.'
