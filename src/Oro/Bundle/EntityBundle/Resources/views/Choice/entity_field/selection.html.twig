<% var field = context.getFieldData(id); %><% if (!_.isUndefined(field.relation_type)) { %><%= _.escape(field.related_entity_plural_label) %>&#32;(<b><%= _.escape(text) %></b>)<% } else { %><%= _.escape(text) %><% } %>
