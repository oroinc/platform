<% _.each(context.splitFieldId(id), function (val, index, list) { %>&#32;<%= _.escape(val.entity.label) %>&nbsp;<b><%= _.escape(val.label) %></b>&nbsp;<% if (index < list.length - 1) { %>><% } %><% }) %>
