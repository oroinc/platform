oro:
    pagination:
        page: Page:
        total: of {{ pages }} | Total of {{ records }} records
    comment:
        plural_label: Comment
        entity_plural_label: Comments
        action:
            delete: Delete Comment
            update: Update Comment
        from:
            button:
                add_comment:
                    label: Add Comment
                edit_comment:
                    label: Save Comment
                cancel_comment:
                    label: Cancel
        attachment:
            tooltip: Attached file {{ filename }}
            delete_message: The comment attachment is successfully removed
        item:
            minutes_ago: '{0}now|{1}{{ number }} minute ago|[2,+Inf]{{ number }} minutes ago'
            hours_ago: '{1}{{ number }} hour ago|[2,+Inf]{{ number }} hours ago'
            days_ago: '{1}{{ number }} day ago|[2,+Inf]{{ number }} days ago'
            months_ago: '{1}{{ number }} month ago|[2,+Inf]{{ number }} months ago'
            years_ago: '{1}over {{ number }} year ago|[2,+Inf]over {{ number }} years ago'
            added: '{{ user }} added {{ time }}'
            update: 'last edited by {{ user }} {{ time }}'
