{{ oro_widget_render({
        'widgetType': 'dashboard-item',
        'wid': 'dashboard-widget-' ~ uid ~ '-' ~ widget.widget.id,
        'url': path(widget.config.route, widget.config.route_parameters),
        'state': {
                'layoutPosition': widget.widget.layoutPosition,
                'expanded': widget.widget.expanded
        }
   })
}}
