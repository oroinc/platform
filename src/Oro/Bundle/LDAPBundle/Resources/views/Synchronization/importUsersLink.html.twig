{% import 'OroUIBundle::macros.html.twig' as UI %}

{{ UI.clientButton({
    'dataUrl': path('oro_ldap_import_users'),
    'aCss': 'no-hash',
    'label': 'oro.ldap.action.import_users'|trans,
    'title': 'oro.ldap.action.import_users'|trans,
    'widget' : {
        'type' : 'dialog',
        'multiple' : false,
        'reload-grid-name' : 'users-grid',
        'options' : {
            'alias': 'import-ldap-users-dialog',
            'dialogOptions' : {
                'title' : 'oro.ldap.action.import_users'|trans,
                'allowMaximize': true,
                'allowMinimize': true,
                'dblclick': 'maximize',
                'maximizedHeightDecreaseBy': 'minimize-bar',
                'width': 1000
            }
        }
    }
}) }}
