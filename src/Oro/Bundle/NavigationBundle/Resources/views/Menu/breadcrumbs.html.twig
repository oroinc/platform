<ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
        <li>
            {{ breadcrumb.label|raw|trans }}{% if not loop.last %}<span class="divider">/</span>{% endif %}
        </li>
    {% endfor %}
</ul>
