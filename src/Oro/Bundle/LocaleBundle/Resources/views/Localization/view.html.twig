{% extends 'OroUIBundle:actions:view.html.twig' %}

{% oro_title_set({params : {"%id%": entity.id}}) %}

{% block pageHeader %}
    {% set breadcrumbs = {
        'entity': entity,
        'indexPath': path('oro_locale_localeset_index'),
        'indexLabel': 'oro.locale.localeset.entity_plural_label'|trans,
        'entityTitle': 'oro.locale.localeset.navigation.view'|trans({
            '%name%': entity.name|default('N/A'|trans)
        })
    } %}
    {{ parent() }}
{% endblock pageHeader %}

{% block content_data %}
    {% set localeWidget %}
        {{ oro_widget_render({
            'widgetType': 'block',
            'title': 'oro.locale.localeset.widgets.localization_information'|trans,
            'url': path('oro_locale_localeset_info', {'id': entity.id})
        }) }}
    {% endset %}

    {% set dataBlocks = [
        {
            'title': 'oro.locale.localeset.sections.general'|trans,
            'class': 'active',
            'subblocks': [{data: [localeWidget]}]
        }
    ] %}

    {% set id = 'localeset-view' %}
    {% set data = {'dataBlocks': dataBlocks} %}

    {{ parent() }}
{% endblock content_data %}
