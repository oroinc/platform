{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set gridName = 'locales-grid' %}
{% set pageTitle = 'oro.locale.locale.entity_plural_label'|trans %}

{% block navButtons %}
    {#{% if resource_granted('oro_locale_locale_create') %}#}
        <div class="btn-group">
            {{ UI.addButton({
                'path': path('oro_locale_locale_create'),
                'entity_label': 'oro.locale.locale.entity_label'|trans
            }) }}
        </div>
    {#{% endif %}#}
{% endblock %}
