This API extends **JSON:API** to support identifiers from an external system.
To use external IDs instead of internal system IDs, include the following header in your request:

&nbsp;&nbsp; **X-Integration-Type**: **ext_id**
