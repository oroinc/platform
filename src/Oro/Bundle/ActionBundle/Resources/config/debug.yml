services:
    oro_action.config.dumper:
        class: Oro\Component\Config\Dumper\CumulativeConfigMetadataDumper
        public: false
        arguments:
            - '%kernel.cache_dir%'
            - '%kernel.debug%'
            - 'oro_action'
