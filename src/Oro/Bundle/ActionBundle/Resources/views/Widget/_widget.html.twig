{% if oro_action_has_buttons(params) %}
    <div class="pull-left">
        {{ oro_widget_render({
            'widgetType': 'block',
            'url': path(oro_action_widget_route(), params),
            'alias': 'action_buttons_widget',
        }) }}
    </div>
{% endif %}
