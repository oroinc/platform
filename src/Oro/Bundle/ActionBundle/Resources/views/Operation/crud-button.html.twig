{% extends 'OroActionBundle:Operation:button.html.twig' %}

{% block button %}
    {% set entityLabel = actionData.entityLabel %}
    {% if entityLabel is defined %}
        {% set linkTitle = params.frontendOptions.title|default(params.label)|trans({'%entityLabel%': entityLabel|trans}) %}
    {% endif %}

    {{ parent() }}
{% endblock %}
