{% set originalValue = record.getValue('key')|trans({}, record.getValue('domain'), 'en') %}
{% if originalValue is empty %}
    {{ record.getValue('key') }}
{% else %}
    {{ originalValue }}
{% endif %}
