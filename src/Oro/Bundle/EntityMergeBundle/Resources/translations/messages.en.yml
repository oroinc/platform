oro:
    entity_merge:
        action:
            merge: Merge {{ label }}

        merge_modes:
            replace: Replace
            merge: Unite

        form:
            master_record: Master Record
            strategy: Strategy
