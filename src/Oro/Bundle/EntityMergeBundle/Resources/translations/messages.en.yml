oro:
    entity_merge:
        controller:
            merged_successful: Entities were successfully merged.
            select_error: Select entities to merge.
        action:
            merge: Merge {{ label }}

        merge_modes:
            replace: Replace
            unite: Append

        form:
            master_record: Master Record
            strategy: Strategy
            select_all: Select All Values
            merge_values: Merge Values
            strategy_tooltip: Choose merge strategy
            master_record_tooltip: Choose master record
            merge_button: Merge
