oro:
    entity_merge:
        controller:
            merged: Entities are successfully merged
            select: Select entities to merge.
        action:
            merge: Merge {{ label }}

        merge_modes:
            replace: Replace
            merge: Unite

        form:
            master_record: Master Record
            strategy: Strategy
