oro:
    entity_merge:
        form:
            strategy_tooltip:  “Replace” strategy allows to value of selected field. With “Append” strategy selected value will be appended to master record value.
            master_record_tooltip: Field values from all records will be merged to Master Record, all other records will be deleted.
