oro_layout:
    actions:
        - @add:
            id:        success_message
            parentId:  content
            blockType: embed_form_success
            options:
                message: { @value: $data.embedded_form_entity.successMessage }
                form_id: { @value: $data.embedded_form_entity.id }
