security:
    firewalls:
        embedded_form:
            pattern: ^/embedded-form/(submit|success)
            provider: chain_provider

oro_security:
    access_control:
        - { path: ^/embedded-form/(submit|success), roles: PUBLIC_ACCESS }
