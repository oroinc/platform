doctrine:
    orm:
        entity_managers:
            config:
                mappings:
                    OroEntityConfigBundle:
                        mapping:   true
                        is_bundle: true
