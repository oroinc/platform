doctrine:
    orm:
        entity_managers:
            config:
                mappings:
                    OroEntityConfigBundle:
                        mapping:   true
                        is_bundle: true

oro_message_queue:
    persistent_services:
        - 'oro_entity_config.cache'
