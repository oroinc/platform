{% extends 'OroEntityExtendBundle:ConfigFieldGrid:create.html.twig' %}

{% oro_title_set({params : {'%title%': 'oro.entity_extend.attribute.new_attribute'|trans }}) %}

{% block navButtons %}
    {{ UI.cancelButton(path('oro_attribute_index', {alias: entityAlias})) }}
    {{ UI.saveActionButton({
        'label': 'Continue'|trans,
        'route': 'oro_attribute_save',
        'params': {alias: entityAlias}
    }) }}
{% endblock navButtons %}

{% block pageHeader %}
    {% set breadcrumbs = {
        'entity':      'entity',
        'indexPath':   path('oro_attribute_index', {alias: entityAlias}),
        'indexLabel':  attributesLabel|trans,
        'entityTitle': 'oro.entity_extend.attribute.new_attribute'|trans
    } %}
    {{ parent() }}

{% endblock pageHeader %}
