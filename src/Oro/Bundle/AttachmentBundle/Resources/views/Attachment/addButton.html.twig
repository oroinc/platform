{% import 'OroUIBundle::macros.html.twig' as UI %}

{#{% if resource_granted('oro_attachment_create') %}#}
    {{ UI.clientButton({
        'dataUrl': path('oro_attachment_create', { 'entityClass': oro_class_name(entity), 'entityId': entity.id}) ,
        'aCss': 'no-hash',
        'iCss': 'icon-paper-clip hide-text',
        'label' : 'oro.attachment.action.add'|trans,
        'widget' : {
            'type' : 'dialog',
            'multiple' : false,
            'reload-grid-name' : 'attachment-grid',
            'options' : {
                'dialogOptions' : {
                    'title' : 'oro.attachment.action.add'|trans,
                    'allowMaximize': false,
                    'allowMinimize': false,
                    'maximizedHeightDecreaseBy': 'minimize-bar',
                    'width': 550
                }
            }
        },
    }) }}
{#{% endif %}#}
