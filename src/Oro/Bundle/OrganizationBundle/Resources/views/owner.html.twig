{% if form.owner is defined %}
    {% if form.owner.vars.label == 'oro.organization.businessunit.parent.label' %}
        {{ form_row(form.owner) }}
    {% else %}
        {{ form_row(form.owner, {'label': label is not same as(false) ? label|trans : ''}) }}
    {% endif %}
{% endif %}