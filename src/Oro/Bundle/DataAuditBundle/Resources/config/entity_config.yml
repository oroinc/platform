oro_entity_config:
    dataaudit:
        entity:
            items:
                auditable:
                    options:
                        priority:           60
                        default_value:      false
                        indexed:            true
                    grid:
                        type:               boolean
                        label:              oro.dataaudit.entity_config.entity.items.auditable
                        filter_type:        boolean
                        required:           true
                        sortable:           true
                        filterable:         true
                        show_filter:        false
                    form:
                        type:               choice
                        options:
                            choices:        ['No', 'Yes']
                            empty_value:    false
                            block:          other
                            label:          oro.dataaudit.entity_config.entity.items.auditable
        field:
            items:
                auditable:
                    options:
                        value_type:         boolean
                        priority:           60
                        default_value:      false
                        allowed_type:       [string, text, integer, smallint, bigint, boolean, datetime, date, time, float, decimal, money, percent, enum, multiEnum, manyToOne, manyToMany, array, simple_array, json_array, ref-many, ref-one]
                    grid:
                        type:               boolean
                        label:              oro.dataaudit.entity_config.field.items.auditable
                        required:           true
                        sortable:           true
                    form:
                        type:               choice
                        options:
                            choices:        ['No', 'Yes']
                            empty_value:    false
                            block:          other
                            label:          oro.dataaudit.entity_config.field.items.auditable
