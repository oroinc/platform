oro:
    security:
        share_scopes:
            confirmModal:
                title: 'Removing the sharing scope'
                okText: 'OK'
                cancelText: 'Cancel'
                content: 'Attention: removing the "{{ scope_name }}" sharing scope from "{{ entity_name_plural }}" will result in permanent un-sharing of all existing "{{ entity_name }}" records. This action cannot be undone. Do you want to continue?'
        action:
            shared_success: Sharing settings were successfully applied.
            shared_error: Sorry, share operation failed.
            forbidden_error: You do not have permission to perform this action.
