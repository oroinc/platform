{% macro renderAddress(address) %}
    {% if address.label %}
        <b>{{ address.label|escape }}</b>
        <br />
    {% endif %}
    {{ address|oro_format_address|escape|nl2br }}
{% endmacro %}
