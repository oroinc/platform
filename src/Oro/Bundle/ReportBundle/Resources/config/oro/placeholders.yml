placeholders:
    placeholders:
        segment_criteria_list:
            items:
                aggregated_field_condition:
                    order: 10

    items:
        aggregated_field_condition:
            template: OroReportBundle:Segment:aggregated_field_condition.html.twig
            applicable: "@oro_ui.placeholder.filter->isSame($params.id$, oro_report-condition-builder)"
