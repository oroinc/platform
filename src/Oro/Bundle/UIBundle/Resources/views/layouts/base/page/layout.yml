layout:
    actions:
        - '@add':
            id: scripts_before
            blockType: container
            parentId: body
            prepend: true
        - '@add':
            id: layout_js_build_scripts_after
            blockType: container
            parentId: body

