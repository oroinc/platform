<ul class="nav pull-left application-menu fix_logo_home" style="background-color: unset;">
    <li class="home">
        <a href="{{ path('oro_default') }}"><span>Home</span></a>
    </li>
</ul>

{% if isDesktopVersion() %}
    {% placeholder organization_selector %}
{% else %}
    {% set organization_name %}
        {% placeholder organization_name %}
    {% endset %}
    <span id="main-menu-toggle">
        <i class="icon-menu-bars"></i>
    </span>
    <h1 class="logo">
        <a href="{{ path('oro_default') }}" title="{{ organization_name|striptags|trim }}">
            {% if oro_theme_logo() %}
                <img src="{{ asset(oro_theme_logo()) }}" alt="{{ organization_name|striptags|trim }}">
            {% else %}
                {{ organization_name }}
            {% endif %}
        </a>
    </h1>
{% endif %}
