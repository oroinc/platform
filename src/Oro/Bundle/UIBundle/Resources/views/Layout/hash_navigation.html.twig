{% block root_widget -%}
    {% set data = {
        'scripts': block_widget(block.hash_navigation_data.scripts),
        'content': block_widget(block.hash_navigation_data.main_container),
        'breadcrumb': block_widget(block.hash_navigation_data.breadcrumb)|trim,
        'beforeContentAddition': block_widget(block.hash_navigation_data.before_content_addition)
    } -%}
    {{ oro_get_content(data|default({}))|json_encode|raw }}
{%- endblock %}
