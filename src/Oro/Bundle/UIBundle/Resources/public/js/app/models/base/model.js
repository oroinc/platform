define([
    'chaplin'
], function(Chaplin) {
    'use strict';

    var BaseModel;

    /**
     * @class BaseModel
     * @extends Chaplin.Model
     */
    BaseModel = Chaplin.Model.extend(/** @lends BaseModel.prototype */{

    });

    return BaseModel;
});
