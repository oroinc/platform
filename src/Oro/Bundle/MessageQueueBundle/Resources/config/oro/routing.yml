oro_message_queue_bundle:
    resource:     "@OroMessageQueueBundle/Controller"
    type:         annotation
    prefix:       /message-queue

oro_message_queue_bundle_api:
    resource:     "@OroMessageQueueBundle/Resources/config/oro/routing_api.yml"
    requirements:
        version:  latest|v1
        _format:  json
    defaults:
        version:  latest
