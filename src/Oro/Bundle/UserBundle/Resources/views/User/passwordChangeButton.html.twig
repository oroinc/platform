{% import 'OroUIBundle::macros.html.twig' as UI %}

{{ UI.clientButton({
    'dataUrl': path('oro_user_reset_set_password', {id: entity.id}),
    'aCss': 'no-hash',
    'iCss': 'fa-key',
    'dataId': entity.id,
    'label': 'oro.user.password.change_password.label'|trans,
    'widget': {
        'type': 'dialog',
        'multiple': false,
        'refresh-widget-alias': 'activity-list-widget',
        'options': {
            'alias': 'set-password-dialog',
            'dialogOptions': {
                'title': 'oro.user.password.change_dialog.title'|trans,
                'allowMaximize': true,
                'allowMinimize': true,
                'dblclick': 'maximize',
                'maximizedHeightDecreaseBy': 'minimize-bar',
                'width': 550
            }
        }
    }
}) }}
