{{ UI.clientButton({
    'dataUrl': path('oro_user_reset_send_email_as_admin', {id: entity.id}),
    'aCss': 'no-hash',
    'iCss': 'icon-repeat',
    'dataId': entity.id,
    'label': 'oro.user.password.reset.label'|trans,
    'widget' : {
        'type' : 'dialog',
        'multiple' : false,
        'options' : {
            'alias': 'reset-password-dialog',
            'dialogOptions' : {
                'title' : 'oro.user.password.reset__dialog.title'|trans,
                'allowMaximize': false,
                'allowMinimize': false,
                'autoResize': true,
                'width': 550
            }
        }
    }
}) }}
