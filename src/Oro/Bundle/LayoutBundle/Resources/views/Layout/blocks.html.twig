{% block container_widget -%}
    {% for child in children -%}
        {% if not child.rendered -%}
            {{ block_widget(child) }}
        {%- endif %}
    {%- endfor %}
{%- endblock container_widget %}
