# tests searching entity which have configured field without `target_fields` parameter
request:
    search: 'watt'
    offset: ~
    max_results: ~
response:
    records_count: 1
    count: 1
    rest:
        data:
            -
                entity_name: Oro\Bundle\TestFrameworkBundle\Entity\Product
                entity_label: oro.testframework.product.entity_label
                record_string: "500-watt Work Light"
                record_url: http://localhost%oro_default_prefix%search/
    soap:
        item:
            -
                entityName: Oro\Bundle\TestFrameworkBundle\Entity\Product
                entityLabel: oro.testframework.product.entity_label
                recordTitle: "500-watt Work Light"
                recordUrl: http://localhost%oro_default_prefix%search/
                selectedData: []
