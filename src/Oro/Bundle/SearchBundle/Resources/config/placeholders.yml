placeholders:
    navbar:
        items:
            search:
                order: 200

items:
   search:
        action: OroSearchBundle:Search:searchBar
        applicable: "@oro_user.placeholder.filter->isUserApplicable()"
