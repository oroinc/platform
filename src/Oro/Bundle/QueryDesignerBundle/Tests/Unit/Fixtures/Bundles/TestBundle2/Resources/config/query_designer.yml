query_designer:
    filters:
        filter1:
            applicable: [{type: text}]
        filter3:
            type:       number
        filter4:
            query_type: [type2]

    grouping:
        exclude: [{type: array}]

    aggregates:
        aggregate1:
            applicable: [{type: float}]
            functions:  [{name: count, expr: COUNT($column)}]
