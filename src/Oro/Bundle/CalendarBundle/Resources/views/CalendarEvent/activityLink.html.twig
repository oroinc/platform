{{ UI.clientLink({
    'dataUrl': path(
        'oro_calendar_event_create', {
            entityClass: oro_class_name(entity, true),
            entityId: entity.id
    }),
    'aCss': 'no-hash',
    'iCss': 'icon-time',
    'dataId': entity.id,
    'label' : 'oro.calendar.add_event'|trans,
    'widget' : {
        'type' : 'dialog',
        'multiple' : true,
        'reload-grid-name' : 'activity-calendar-event-grid',
        'options' : {
            'alias': 'calendar-event-dialog',
            'dialogOptions' : {
                'title' : 'oro.calendar.add_event'|trans,
                'allowMaximize': true,
                'allowMinimize': true,
                'dblclick': 'maximize',
                'maximizedHeightDecreaseBy': 'minimize-bar',
                'autoResize': true,
                'width': 475
            }
        }
    }
}) }}
