{% block oro_calendar_choice_row %}
    {% if choices is not empty %}
        {% if choices|length > 1 %}
            {{ form_row(form) }}
        {% else %}
            {{ form_row(form, {'label': false}) }}
        {% endif %}
    {% endif %}
{% endblock %}
