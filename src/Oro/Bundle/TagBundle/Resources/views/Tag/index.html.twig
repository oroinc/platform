{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set gridName = 'tag-grid' %}
{% set pageTitle = 'oro.tag.entity_plural_label'|trans %}

{% block navButtons %}
    {% if resource_granted('oro_tag_create') %}
        {{ UI.addButton({
            'path' : path('oro_tag_create'),
            'title' : 'Create'|trans ~ ' ' ~ 'oro.tag.entity_label'|trans|lower,
            'label' : 'Create'|trans ~ ' ' ~ 'oro.tag.entity_label'|trans|lower
        }) }}
    {% endif %}
{% endblock %}
