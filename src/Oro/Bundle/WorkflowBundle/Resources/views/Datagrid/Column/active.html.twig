{% set config = oro_entity_config(record.getValue('relatedEntity'), 'workflow') %}
{% set workflowName = record.getValue('name') %}
{% set activeWorkflows = config.active_workflows|default([]) %}
{% if workflowName in activeWorkflows %}
    {{ 'Yes'|trans }}
{% else %}
    {{ 'No'|trans }}
{% endif %}
