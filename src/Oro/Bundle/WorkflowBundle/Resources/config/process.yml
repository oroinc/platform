definitions:
    test_definition:
        label: 'Test Definition'
        enabled: true
        entity: Oro\Bundle\UserBundle\Entity\User
        order: 20
        actions_configuration:
            - @assign_value: [$namePrefix, 'Pr.']

    another_definition:
        label: 'Another definition'
        entity: My\Entity
        actions_configuration: []

    contact_definition:
        label: 'Contact Definition'
        enabled: true
        entity: OroCRM\Bundle\ContactBundle\Entity\Contact
        order: 20
        actions_configuration:
            - @assign_value: [$namePrefix, 'Pr.']

triggers:
    test_definition:
        -
            event: update
            field: birthday
            queued: true
            time_shift: 60
        -
            event: update
            field: loginCount
            queued: true
            time_shift: 60
        -
            event: create
            queued: true
            time_shift: P6YT5M
        -
            event: delete
            queued: true

    contact_definition:
        -
            event: update
            field: reportsTo
            queued: true
            time_shift: 60
