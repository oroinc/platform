<div class="field">
    {{ form_widget(repo.type) }}
    {{ form_widget(repo.url, {'attr': {'class': 'selector', 'placeholder': 'URL'}}) }}

    <a href="#" onclick="javascript:$(this).parent('div').remove();" class="delete-action" title="{{ 'oro.distribution.action.delete'|trans }}">
        <i class="fa-trash-o hide-text">{{ 'oro.distribution.action.delete'|trans }}</i>
    </a>
    {{ form_errors(repo.url) }}
</div>