{% extends 'OroDataGridBundle:Grid/widget:widget.html.twig' %}

{% block widget_content %}
    {% set renderParams = renderParams|merge({
        routerEnabled: false,
        requireJSModules: ['orodatagrid/js/row-select-for-widget/builder'],
        gridBuildersOptions: {
            rowSelectForWidget: {
                wid: app.request.get('_wid'),
                multiSelect: multiselect
            }
        }
    }) %}

    {{ parent() }}
{% endblock %}
