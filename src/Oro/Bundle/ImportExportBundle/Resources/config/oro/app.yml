knp_gaufrette:
    adapters:
        importexport:
            local:
                directory: '%kernel.root_dir%/%oro_importexport.importexport_dir%'
    filesystems:
        importexport:
            adapter:    importexport
            alias:      importexport_filesystem
